server:
  # Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
  log_level: INFO

# Telegram notifications
telegram:
  bot_token: TELEGRAM_BOT_TOKEN
  chat_id: TELEGRAM_CHAT_ID

# Trading accounts
accounts:
  account-a:
    api_token: tinkoff_token_1
    account_id: account_id_1
    # true for sandbox, false for real trading
    sandbox_mode: true
  account-b:
    api_token: tinkoff_token_2
    account_id: account_id_2
    sandbox_mode: false
